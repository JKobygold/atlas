# Atlas

A personal daily execution app — an "external prefrontal cortex" that answers *what do I do right now*.
Static, offline-first PWA. No accounts, no backend: all personal data stays in the browser on your device.

Add to Home Screen on iPhone (Safari → Share → Add to Home Screen) to install it.
